# Orbit Sentinel — Quick Setup

This script installs dependencies and starts the visualizer with demo mode.

Run this script, then:
1. Open the URL shown in your browser
2. Press **Space** to start/stop the auto-play demo
3. Or visit `http://localhost:5173/?demo=true` for auto-demo on load

```powershell
.\setup.ps1
```
